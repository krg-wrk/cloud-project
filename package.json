{
  "name": "cloud-project",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "setup": "node tools/setup.mjs",
    "configure": "node tools/configure.mjs",
    "doctor": "node --env-file-if-exists=.env tools/doctor.mjs",
    "refresh": "node tools/refresh.mjs",
    "build": "npm run build -w server && npm run build -w client",
    "start": "npm run start -w server",
    "lint": "npm run lint -w client",
    "typecheck": "tsc -b client",
    "test": "npm test -w server && npm test -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
